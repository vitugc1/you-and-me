import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  input: {
    width: '90%',
    height: 56,
    backgroundColor: '#fff',
    borderRadius: 5,
    fontSize: 14,
    padding: 7,
    paddingLeft: 20,
    marginBottom: 12,
    borderWidth: 1,
    color: '#000',
  }
})